\chapter{Implementation}

\section{Development Environment and Tools}

The framework is implemented in Python 3.10 with PyTorch as the deep learning backend. Table~\ref{tab:libraries} lists the principal libraries and what they do.

\begin{table}[h]
\centering

\caption{Key libraries and their roles in the framework.}

\label{tab:libraries}
\begin{tabular}{ll}
\hline
\textbf{Library} & \textbf{Purpose} \\

\hline
PyTorch & Neural network definition, training, and inference \\
PyTorch Geometric & Graph convolution and attention layers (GCN, GAT) \\
python-chess & Board representation, move generation, engine protocol \\
PyArrow & Parquet file I/O for the training dataset \\
NumPy / Pandas & Numerical operations and data analysis \\
Matplotlib & Evaluation flow charts and training curve visualisation \\
PyYAML & Configuration file parsing \\
tqdm & Progress bars for training \\
pydantic-settings & Parsing of config yaml \\
\hline

\end{tabular}
\end{table}

All experiments ran on two hardware configurations: an Apple M-series workstation using the MPS (Metal Performance Shaders) backend for local development, and an NVIDIA GPU (CUDA) for main training. The codebase auto-detects the best available device at startup - preferring CUDA to MPS over CPU - and enables hardware-specific optimisations (TF32 math and cuDNN auto-tuning on CUDA).

Version control uses Git. All hyperparameters live in a single YAML configuration file (\texttt{config/settings.yaml}), parsed at runtime into a hierarchy of Python dataclasses (\texttt{Config}, \texttt{ModelConfig}, \texttt{TrainingConfig}, etc.), so every experiment is fully reproducible from its configuration snapshot.

\section{Core Framework Implementation}

\subsection{Abstract Base Classes}

The framework's extensibility rests on two abstract base classes:

\begin{itemize}
    \item \texttt{ChessModel(ABC, nn.Module)}: Every neural network backbone subclasses this and implements \\ \texttt{forward\_backbone(x)} (returning a fixed-dimension feature vector) and \texttt{get\_backbone\_output\_dim()}. The base class provides a concrete \texttt{forward(x)} that chains the backbone with whichever output head has been attached via \texttt{set\_head()}.
    \item \texttt{StateEncoder(ABC)}: Every board encoder implements \texttt{encode(board)} and \texttt{get\_input\_shape()}. This lets the data pipeline and agents work with any encoder-model pairing without type-checking.
\end{itemize}

% TODO: Write more - discuss a concrete example of adding a new backbone, e.g. how many lines of code it takes and what the developer experience is like

\subsection{Factory Pattern}

The \texttt{create\_model(config)} factory reads the backbone name and head type from the configuration, instantiates the appropriate backbone with its architecture-specific parameters (e.g., number of residual blocks, number of attention heads), constructs the matching output head, and attaches it. 
\\
A companion \texttt{get\_encoder\_for\_model(backbone)} factory maps backbone names to their corresponding encoder classes. This two-step factory keeps backbone-encoder pairings consistent.

\subsection{Configuration System}
All tunable parameters are centralised in a YAML file and parsed into nested dataclasses:
\begin{itemize}
    \item \texttt{HardwareConfig}: Device selection and data-loader worker count.
    \item \texttt{ModelConfig}: Backbone type, head type, and per-family sub-configs (\texttt{CNNConfig}, \texttt{TransformerConfig}, \texttt{GNNConfig}).
    \item \texttt{TrainingConfig}: Batch size, learning rate, weight decay, epoch count, loss weights, and LR scheduler type.
    \item \texttt{EngineConfig}: Path to the Stockfish binary and default search parameters.
\end{itemize}

Pydantic-settings parses the yaml file and allows us to access the config options anywhere in the project.

\section{Supervised Learning Model}

\subsection{Data Preparation}

Training data comes from the Lichess Stockfish evaluation dataset, which provides millions of positions annotated with engine analysis. The data is stored in Apache Parquet format for efficient columnar compression and row-group-level random access.

The \texttt{ChessDataset} class, implemented as a PyTorch \texttt{IterableDataset}, the following Parquet schema:

\begin{enumerate}
    \item \textbf{PV-unrolled:} Each row contains a FEN string, the best move in UCI notation, a precomputed value. It is structured as f, b, v which was artifact of converting jsonl to parquet when doing the data-cleanup/preparation. Where the fen represents the current board state and the b is the best move from the board state and v is a value from +1 to -1 depending on the side that is currently winning.
\end{enumerate}

Data loading works in following steps. First, the dataset lists all row-groups across the Parquet file. These are partitioned across DataLoader workers by worker ID. Finally, each row is converted to a training example by encoding the board and building the policy and value targets.

\textbf{Policy targets} are constructed in \emph{hard label} mode, the best move receives a probability of 1.0 whereas everything else is set to 0 in the super sparse 4544-dimensional vector.

\textbf{Value targets} are taken from the dataset: the game result ($+1$, $0$, $-1$), the mate score (mapped to $\pm 1$), or the centipawn evaluation (divided by 1000 and clamped to $[-1, 1]$). The value is always expressed based on the colour that is winning.

A custom \texttt{collate\_fn} batches examples into the format expected by each model family. CNN inputs are stacked along the batch dimension. Transformer inputs have each dictionary field (tokens, positions, masks) stacked independently. GNN inputs require more care: edge indices are concatenated with per-graph offsets of 64 nodes, and a \texttt{batch} vector maps each node to its originating graph, following the PyTorch Geometric batching convention.

% TODO: Write more - note any data quality issues encountered (e.g., malformed FENs, inconsistent PV lines) and how they were handled

\subsection{Neural Network Architecture}

Six backbone architectures are implemented, two per model family.

\subsubsection{Convolutional Neural Networks}

\textbf{ConvNet} consists of six sequential convolutional blocks, each comprising a $3 \times 3$ convolution (with padding to preserve spatial dimensions), batch normalisation, and ReLU activation. The first block projects the 18-channel input to 256 channels; subsequent blocks maintain this width. A global average pooling layer reduces the $256 \times 8 \times 8$ feature maps to a 256-dimensional vector.

\textbf{ResNet} begins with an initial $3 \times 3$ convolution that projects the input to 256 channels, followed by $N$ residual blocks (configurable to 6, 10, or 20). Each residual block contains two $3 \times 3$ convolutions with batch normalisation and a SE block, and the input is added to the output via a skip connection before the final SiLU. Global average pooling produces the output vector.

\subsubsection{Transformers}

\textbf{SquareTransformer} tokenises the board as 64 fixed tokens (one per square) using a vocabulary of 13 (empty + 12 piece types). Each token is embedded and summed with a learnable positional embedding and a coordinate projection (normalised rank-and-file). Side-to-move information is broadcast as a bias to all tokens, while castling rights are injected into a prepended CLS token. The sequence passes through $N$ Transformer encoder blocks, each using pre-LayerNorm, multi-head self-attention (8 heads), and a GELU-activated feed-forward network with a $4\times$ expansion ratio. The CLS token's final hidden state serves as the backbone output.

\textbf{PieceTransformer} tokenises the board as a variable-length sequence of up to 32 tokens (one per piece). Each token encodes the piece type (vocabulary of 12) and its square position via separate embedding tables. A key-padding mask prevents attention from flowing to padded positions. A CLS token aggregates the sequence representation.

\subsubsection{Graph Neural Networks}

\textbf{GCN} projects the 14-dimensional node features to a hidden dimension of 256 via a linear layer, then applies $N$ GCN layers. Each layer performs a graph convolution (\texttt{GCNConv}), batch normalisation, and ReLU activation with a residual connection. Global mean pooling aggregates node embeddings into a single graph-level vector, which is summed with a side-to-move embedding and a castling projection before a final linear output layer.

\textbf{GAT} follows the identical structure but replaces \texttt{GCNConv} with \texttt{GATConv}, which computes multi-head attention (4 heads) over neighbours. The GAT layers accept the 3-dimensional edge attributes (distance, ray-tracing, legality) via the \texttt{edge\_dim} parameter, allowing the attention mechanism to weight edges by their tactical significance. ELU activation replaces ReLU for smoother gradients in the attention computation.

% TODO: Write more - discuss any training instabilities observed with GAT (e.g., attention collapse) and how edge attributes helped

\subsection{Training Process}
All the bash scripts needed to reproduce the training runs - with the exact configs listed below - live in scripts/training.
% add a table with all hyperparameters for reproducibility
\begin{table}[h]
\centering

\caption{Training configuration parameters used across all experiments.}

\label{tab:training-config}
\begin{tabular}{ll}
\hline
\textbf{Hyperparameter} & \textbf{Value} \\

\hline
Batch Size (Default) & 2048 \\
Batch Size (GAT) & 1024 \\
Data Size & 10M, 100M, 200M, 500M, 1B \\
Epochs (per data size) & 50, 15, 10, 5, 1 \\
Learning Rate & 0.001 \\
Weight Decay & 0.0001 \\
Policy Loss Weight & 1.0 \\
Value Loss Weight & 1.0 \\
Gradient Accumulation Steps & 1 \\
Gradient Clip Max Norm & 1.0 \\
LR Scheduler & Cosine \\
LR Step Size & 10 \\
LR Gamma & 0.1 \\
Device & CUDA (8 workers) \\
\hline

\end{tabular}
\end{table}

\subsubsection{Loss Functions}

Three loss modules are provided:

\begin{itemize}
    \item \textbf{PolicyLoss:} Cross-entropy for hard labels, with optional label smoothing.
    \item \textbf{ValueLoss:} Mean squared error between the predicted value (tanh-bounded) and the target.
    \item \textbf{DualLoss:} Weighted sum $\mathcal{L} = \lambda_p \mathcal{L}_{\text{policy}} + \lambda_v \mathcal{L}_{\text{value}}$.
\end{itemize}

\subsubsection{Optimisation}

Training uses the AdamW optimiser with a default learning rate of $10^{-3}$ and weight decay of $10^{-4}$. Learning rate scheduling supports cosine annealing (default) and step decay. Gradients are clipped to a maximum norm of 1.0 to prevent instability during early training.

\subsubsection{Mixed Precision and Compilation}

On CUDA devices, training uses float16 autocast with \texttt{GradScaler} to maintain numerical stability. On MPS (Apple Silicon), autocast is enabled but the scaler is disabled - MPS doesn't support it. Non-GNN backbones are wrapped with \texttt{torch.compile} for kernel fusion and operator-level optimisation. GNN models had to be excluded from compilation because their dynamic graph structures prevent effective tracing.

\subsubsection{Checkpointing}

After each epoch, the trainer serialises the model state dict, the optimiser state, the scheduler state, the epoch number, the global step count, and the best validation loss. At the end of training, intermediate checkpoints are pruned to keep only every fifth epoch, reducing storage overhead while preserving enough granularity for post-hoc analysis.

\section{Architectural Enhancements}
% maybe write why changes were made kind of hard to showcase diagram for the same
After the initial implementation and early training runs, several architectural improvements addressed weaknesses I'd noticed.

One of the main weaknesses that was addressed was that models played openings almost perfectly. Still, as soon as they reached the middlegame, they would start making mistakes/blunders against Stockfish, leading to a gradual decline in the evaluation and eventual loss.

By changing the policy head to use more of the model's spatial feature improved that. With SE blocks, ResNet can prioritise different goals at different parts of the game, like for example developing in the opening, king safety in the middle game, and pawn promotion in the end game. Activation function was changed to SiLU because the majority of the positions cluster around 0. \ref{fig:dataset_analysis}
% TODO: Write more - discuss the encoding time overhead of computing these extra planes and whether it was worth it in practice

\subsection{Spatial Policy Head}

The policy head uses spatial features when available: a $1 \times 1$ convolution reduces channels to $C'$, batch norm + ReLU is applied, then the $8 \times 8$ grid is flattened to a $64C'$ vector and mapped by a linear layer to 4544 logits. If spatial features are not available, it falls back to a two-layer MLP on the backbone vector.

\subsection{Squeeze-and-Excitation Blocks}

SE blocks were inserted into each \texttt{ResidualBlock}. After the second convolution and batch normalisation, the feature maps are squeezed via global average pooling to a channel vector of dimension $C$, passed through a bottleneck (\texttt{Linear($C$, $C/r$)} $\rightarrow$ ReLU $\rightarrow$ \texttt{Linear($C/r$, $C$)} $\rightarrow$ Sigmoid, with reduction ratio $r = 16$), and the resulting channel weights multiply the original feature maps. The SE block sits inside the residual path, so the skip connection ensures stable gradient flow even in deep (20-block) configurations.

\subsection{Activation Functions}

ReLU activations in the ConvNet and ResNet backbones were replaced with SiLU (Swish), which gives smoother gradients near zero. The Transformer blocks already used GELU, so no change was needed there.

\section{Data Pipeline Evolution}

The initial streaming Parquet pipeline worked well for prototyping but became the training bottleneck at scale.

\subsection{Stage 1: Filtering and Deduplication}
\label{sec:initial_pipeline}

DuckDB's out-of-core SQL engine normalises FEN strings (stripping halfmove and fullmove counters), groups by the normalised FEN, and keeps only the row with the highest analysis depth or node count. This eliminates redundant positions that would otherwise bias the training distribution toward common openings. Also removes any entries that didn't meet the criteria of >= 18 depth or had a centipawn evaluation that fell outside the range of ± 4000.

\subsection{Stage 2: PV Line Unrolling}
For each base position, the first $N$ moves of the principal variation are played out using \texttt{python-chess}. Each intermediate position is emitted as a separate training example, with the subsequent PV move as its policy target. The root evaluation inherits sign flips across alternating sides - avoiding the prohibitive cost of re-evaluating every unrolled position with Stockfish. A drift analysis validated this: 96\% of inherited evaluations stayed within $\pm 100$ centipawns of the independently computed value.

\subsection{Stage 3: Deduplication and Target Computation}
Positions were normalished again since during the PV unrolling we could have arrived at the same position, in which case the position with the higher depth count or knodes count was kept and others were discarded. By doing this we were able to go from about 3 billion positions to 1.8 billion positions. Engine centipawn scores are converted value targets as ± 1 which are stored as \texttt{float16} (2 bytes).

% TODO: Write more - discuss the tradeoffs in shard size selection and any I/O bottlenecks encountered during training

\section{Baseline Models}

Two baselines anchor the evaluation:

\begin{itemize}
    \item \textbf{RandomAgent:} Plays uniformly at random from the legal move set. It's the lower bound - if a trained model can't beat random, something has gone wrong.
    \item \textbf{UCIAgent (Stockfish):} Wraps Stockfish at configurable depth and skill levels. At full strength (depth 15+), Stockfish sets the upper bound. At reduced depths (1-5), it provides intermediate calibration points for estimating Elo.
\end{itemize}

\section{Benchmarking and Evaluation Pipeline}

The benchmarking system automates model evaluation through three stages:

\begin{enumerate}
    \item Each trained model plays a configurable number of games against Stockfish, alternating colours for fairness.
    \item After every move, Stockfish analyses the resulting position at depth 18, recording the centipawn score from White's perspective. Mate scores are converted to large constants ($\pm 10000$).
    \item Each game is exported in Portable Game Notation with embedded evaluation comments, enabling review in standard chess software (Lichess, Chess.com, ChessBase).
    %\item Matplotlib generates evaluation flow charts for each game, plotting centipawn score against move number with win/draw/loss shading. A combined comparison plot overlays all models.
    %\item A Markdown report is generated with win/draw/loss tables, timing statistics (mean, median, min, max), and links to PGN and figure files.
\end{enumerate}

\section{Documentation}
% expand with more information on how to run certain things
The codebase follows a consistent documentation standard: every module, class, and public method has a docstring specifying its purpose, arguments, and return values. A \texttt{README.md} covers installation, a quick-start guide, and example training commands. The \texttt{setup.sh} script automates environment setup and dependency installation.

Reproducibility was kept in mind throughout the project. Every experiment is fully defined by its configuration snapshot - copy the \texttt{config/settings.yaml} that produced a checkpoint, and you can re-run the exact same training schedule from scratch. The \texttt{README.md} also documents the expected directory layout under \texttt{runs/}, so someone picking up the codebase knows where checkpoints, PGN files, and benchmark outputs land without having to trace the code. 

Shell scripts in \texttt{scripts/training/} and \texttt{scripts/benchmark/} encode the exact commands used for every model variant, meaning there's no reliance on undocumented command-line flags or manually remembered arguments. On top of that, a \texttt{download\_models.sh} script lets anyone pull the pre-trained checkpoints directly. 

The dataset is also pre-processed and can be accessed at 
\\
\texttt{https://www.kaggle.com/datasets/bhavyasingh2611/lichess-evals-stripped} allowing you to download the dataset, puzzles and be able to train/benchmark the models.
% TODO: Write more - discuss documentation for reproducibility (e.g., how experiment configs are versioned), any auto-generated API docs, and how the documentation evolved as the project grew
